{
  "name": "mongo-move",
  "version": "2.0.0",
  "description": "",
  "main": "lib",
  "scripts": {
    "build": "babel src --out-dir lib --ignore test.js,__mocks__ --copy-files",
    "lint": "eslint src --fix --format=node_modules/eslint-formatter-pretty",
    "pretest": "docker-compose up --build -d",
    "test": "MONGO_URL=mongodb://localhost:37017/test jest --forceExit",
    "prepublish": "yarn build",
    "posttest": "docker-compose stop"
  },
  "files": [
    "lib/"
  ],
  "author": "Vlad Holubiev",
  "license": "UNLICENSED",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-plugin-transform-async-to-generator": "^6.24.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.0",
    "eslint": "^4.8.0",
    "eslint-config-google": "0.6.0",
    "eslint-formatter-pretty": "^1.3.0",
    "jest": "^21.2.1",
    "mongodb": "^2.2.31"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "engines": {
    "node": ">=6"
  }
}
